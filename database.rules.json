{
  "rules": {
    "Users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "LiveLocations": {
      "$sessionId": {
        ".read": true,
        ".write": "auth != null",
        ".indexOn": ["startTime"]
      }
    }
  }
}
